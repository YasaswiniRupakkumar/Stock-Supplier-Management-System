# Stock-Supplier-Management-System
A Java-based inventory management system that tracks items and dealers using a custom-built HashMap implementation with separate chaining. The system supports CRUD operations, automatic resizing, and persistent data loading from text files. Designed with a singleton repository pattern for centralized data access.
